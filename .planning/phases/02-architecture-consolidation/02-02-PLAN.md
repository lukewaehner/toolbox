---
phase: 02-architecture-consolidation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/main.rs
  - src/modules/network_tools/controller/mod.rs
  - src/modules/password_manager/controller/mod.rs
  - src/network_tools.rs
  - src/password_manager.rs
  - src/system_utilities.rs
autonomous: true
requirements:
  - ARCH-04

must_haves:
  truths:
    - "src/network_tools.rs is deleted"
    - "src/password_manager.rs is deleted"
    - "src/system_utilities.rs is deleted"
    - "network_tools and password_manager controllers import from src/modules/ model paths"
    - "cargo check passes with no new errors after all deletions"
  artifacts:
    - path: "src/main.rs"
      provides: "Crate root with no mod declarations for network_tools, password_manager, or system_utilities"
      contains: "use crate::modules::network_tools"
    - path: "src/modules/network_tools/controller/mod.rs"
      provides: "Updated controller importing ping from MVC model"
      contains: "crate::modules::network_tools::model"
    - path: "src/modules/password_manager/controller/mod.rs"
      provides: "Updated controller importing save_password and PasswordEntry from MVC model"
      contains: "crate::modules::password_manager::model"
  key_links:
    - from: "src/main.rs"
      to: "src/modules/network_tools/model/mod.rs"
      via: "use crate::modules::network_tools::model"
      pattern: "use crate::modules::network_tools"
    - from: "src/modules/network_tools/controller/mod.rs"
      to: "src/modules/network_tools/model/mod.rs"
      via: "use crate::modules::network_tools::model"
      pattern: "crate::modules::network_tools"
    - from: "src/main.rs"
      to: "src/modules/password_manager/model/mod.rs"
      via: "use crate::modules::password_manager::model"
      pattern: "use crate::modules::password_manager"
    - from: "src/modules/password_manager/controller/mod.rs"
      to: "src/modules/password_manager/model/mod.rs"
      via: "use crate::modules::password_manager::model"
      pattern: "crate::modules::password_manager"
    - from: "src/main.rs"
      to: "src/modules/system_utilities/model/mod.rs"
      via: "use crate::modules::system_utilities::model"
      pattern: "use crate::modules::system_utilities"
---

<objective>
Rewire the three simple legacy modules (network_tools, password_manager, system_utilities) to import from their src/modules/ MVC model counterparts, then delete the three root-level legacy files.

Purpose: The Phase 1 audit confirmed these three files are byte-for-byte copies of their MVC model equivalents. All executable content already exists in src/modules/*/model/. The only work is import site updates (two per module) followed by deletion.

Output: src/network_tools.rs, src/password_manager.rs, and src/system_utilities.rs deleted; all imports route through src/modules/ paths; cargo check clean.
</objective>

<execution_context>
@/Users/lukewaehner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukewaehner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit/AUDIT.md
@.planning/phases/02-architecture-consolidation/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire all three legacy module imports in main.rs and MVC controllers</name>
  <files>src/main.rs, src/modules/network_tools/controller/mod.rs, src/modules/password_manager/controller/mod.rs</files>
  <action>
Make the following targeted import changes. All of these are simple path substitutions — do not change any logic.

IN src/main.rs:
1. Remove `mod network_tools;` (line 19)
2. Remove `mod password_manager;` (line 20)
3. Remove `mod system_utilities;` (line 21)
4. Replace `use network_tools::{ping, SpeedTestResult};` (line 32) with:
   `use crate::modules::network_tools::model::{ping, SpeedTestResult};`
5. Replace `use password_manager::{save_password, PasswordEntry};` (line 34) with:
   `use crate::modules::password_manager::model::{save_password, PasswordEntry};`
6. Replace `use system_utilities::SystemMonitor;` (line 46) with:
   `use crate::modules::system_utilities::model::SystemMonitor;`

Note: main.rs also calls network_tools functions directly at runtime (lines 719, 727, 747, 763, 771 via `network_tools::measure_speed()`, `network_tools::parallel_speed_test()`, etc.). These bare-module-path calls must also be updated to use the full module path. Search for all occurrences of `network_tools::` in main.rs (not the `use` line) and replace with `crate::modules::network_tools::model::`.

IN src/modules/network_tools/controller/mod.rs:
1. Replace `use crate::network_tools::ping;` (line 2) with:
   `use crate::modules::network_tools::model::ping;`

IN src/modules/password_manager/controller/mod.rs:
1. Replace `use crate::password_manager::{save_password, PasswordEntry};` (line 2) with:
   `use crate::modules::password_manager::model::{save_password, PasswordEntry};`

After all changes, verify no remaining legacy bare references:
`grep -rn "use network_tools::\|use password_manager::\|use system_utilities::\|crate::network_tools\|crate::password_manager\|crate::system_utilities" src/`

Expected: zero matches.
  </action>
  <verify>Run `cargo check 2>&1 | grep "error\[" | head -20`. Run `grep -rn "mod network_tools;\|mod password_manager;\|mod system_utilities;" /Users/lukewaehner/Code/Rust/toolbox/src/main.rs` to confirm the mod declarations are gone.</verify>
  <done>cargo check shows no errors. The three `mod` declarations are removed from main.rs. No remaining legacy crate::network_tools, crate::password_manager, or crate::system_utilities references in src/.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the three legacy root-level module files</name>
  <files>src/network_tools.rs, src/password_manager.rs, src/system_utilities.rs</files>
  <action>
Delete all three legacy root-level files:

```
rm /Users/lukewaehner/Code/Rust/toolbox/src/network_tools.rs
rm /Users/lukewaehner/Code/Rust/toolbox/src/password_manager.rs
rm /Users/lukewaehner/Code/Rust/toolbox/src/system_utilities.rs
```

Then run cargo check to confirm the binary compiles cleanly without the deleted files.
  </action>
  <verify>Run `cargo check 2>&1 | grep "error\[" | head -20`. Run `ls /Users/lukewaehner/Code/Rust/toolbox/src/{network_tools,password_manager,system_utilities}.rs 2>&1` to confirm all three are gone.</verify>
  <done>All three files do not exist. cargo check reports zero errors. The deleted files had no unique content (confirmed by Phase 1 audit — all content was already present in src/modules/*/model/).</done>
</task>

</tasks>

<verification>
After both tasks:
1. `cargo check` passes with zero errors
2. `ls src/network_tools.rs src/password_manager.rs src/system_utilities.rs` all return "No such file"
3. `grep -rn "mod network_tools;\|mod password_manager;\|mod system_utilities;" src/main.rs` returns zero matches
4. `grep -rn "crate::network_tools\|crate::password_manager\|crate::system_utilities" src/` returns zero matches
</verification>

<success_criteria>
Three legacy root files deleted; all imports for network_tools, password_manager, and system_utilities route through src/modules/ model paths; cargo check clean.
</success_criteria>

<output>
After completion, create `.planning/phases/02-architecture-consolidation/02-02-SUMMARY.md`
</output>
