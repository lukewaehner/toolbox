---
phase: 02-architecture-consolidation
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/main.rs
  - src/modules/task_scheduler/controller/mod.rs
  - src/modules/system_utilities/controller/mod.rs
  - src/app.rs
  - src/backups/main.bk.rs
autonomous: true
requirements:
  - ARCH-01
  - ARCH-04

must_haves:
  truths:
    - "src/app.rs is deleted"
    - "src/backups/main.bk.rs is deleted"
    - "src/backups/ directory is removed"
    - "mod app; is removed from src/main.rs"
    - "No crate::app:: references remain anywhere in src/"
    - "cargo check passes with zero errors after all deletions"
  artifacts:
    - path: "src/main.rs"
      provides: "Single canonical crate root with no mod app declaration and prepare_status_message as pub(crate)"
      contains: "pub(crate) fn prepare_status_message"
    - path: "src/modules/task_scheduler/controller/mod.rs"
      provides: "Updated controller with no crate::app:: references"
      contains: "crate::prepare_status_message"
    - path: "src/modules/system_utilities/controller/mod.rs"
      provides: "Updated controller with no crate::app:: references"
      contains: "crate::prepare_status_message"
  key_links:
    - from: "src/modules/task_scheduler/controller/mod.rs"
      to: "src/main.rs"
      via: "crate::prepare_status_message"
      pattern: "crate::prepare_status_message"
    - from: "src/modules/system_utilities/controller/mod.rs"
      to: "src/main.rs"
      via: "crate::prepare_status_message"
      pattern: "crate::prepare_status_message"
---

<objective>
Delete the entry-point duplicates (src/app.rs and src/backups/main.bk.rs), remove the mod app; declaration from main.rs, and fix all crate::app:: references in MVC controllers so main.rs is the sole entry point.

Purpose: The Phase 1 audit confirmed app.rs and backups/main.bk.rs are safe to delete (no unique executable content). Two MVC controllers currently depend on crate::app::prepare_status_message — when app.rs is deleted, that path breaks. The fix is to make prepare_status_message pub(crate) in main.rs so controllers can reference it at crate::prepare_status_message.

Output: app.rs and backups/ deleted; mod app; removed; zero crate::app:: references remain; cargo check clean; ARCH-01 and ARCH-04 satisfied.
</objective>

<execution_context>
@/Users/lukewaehner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukewaehner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit/AUDIT.md
@.planning/phases/02-architecture-consolidation/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose prepare_status_message and fix crate::app:: references in MVC controllers</name>
  <files>src/main.rs, src/modules/task_scheduler/controller/mod.rs, src/modules/system_utilities/controller/mod.rs</files>
  <action>
Two MVC controllers import prepare_status_message from crate::app:: — this path only works while mod app; exists in main.rs. Before deleting app.rs, we must fix this dependency.

IN src/main.rs:
1. Find `fn prepare_status_message(` (line 3388 or nearby) and change it to `pub(crate) fn prepare_status_message(`. This makes it accessible from MVC submodules via the crate:: path.
2. Remove `mod app;` — there is no such declaration in main.rs at the top (the audit confirmed main.rs does NOT declare `mod app;` — app.rs is included as a module only from app.rs's own perspective via Cargo). Re-check: run `grep -n "mod app" src/main.rs` first. If `mod app;` does NOT appear in main.rs, skip this step (the deletion of app.rs itself is sufficient — main.rs does not declare it as a module).

NOTE ON mod app: The audit noted that `src/app.rs` is referenced by `src/main.rs` via `mod app;`. Verify this: `grep -n "^mod app" src/main.rs`. If found, remove that line. If NOT found, `app.rs` is included some other way and you must investigate before proceeding.

IN src/modules/task_scheduler/controller/mod.rs:
1. Replace `use crate::app::prepare_status_message;` (line 1) with no line (delete it — we'll use the full path at each call site).
2. Replace all occurrences of `crate::app::prepare_status_message(` with `crate::prepare_status_message(`. There are 4 inline call sites (lines ~159, 165, 182, 188) plus the initial `use` import. Use the call-site full path pattern instead of a `use` import to avoid ambiguity.
   Alternative: replace the `use` line with `use crate::prepare_status_message;` and leave the call sites as `prepare_status_message(...)`.

IN src/modules/system_utilities/controller/mod.rs:
1. Replace `use crate::app::prepare_status_message;` (line 1) with `use crate::prepare_status_message;`

Verify no remaining crate::app:: references:
`grep -rn "crate::app::" src/`

Expected: zero matches.
  </action>
  <verify>Run `cargo check 2>&1 | grep "error\[" | head -20`. Run `grep -rn "crate::app::" /Users/lukewaehner/Code/Rust/toolbox/src/` to confirm zero remaining references.</verify>
  <done>cargo check shows no errors. Zero crate::app:: references remain in src/. prepare_status_message is pub(crate) in main.rs and callable from MVC submodules.</done>
</task>

<task type="auto">
  <name>Task 2: Remove mod app declaration and delete app.rs and backups/</name>
  <files>src/main.rs, src/app.rs, src/backups/main.bk.rs</files>
  <action>
Step 1 — Remove the mod app declaration from main.rs if it exists:
Run `grep -n "^mod app" /Users/lukewaehner/Code/Rust/toolbox/src/main.rs`. If found, remove that line from src/main.rs.

Step 2 — Delete the duplicate entry-point files:
```
rm /Users/lukewaehner/Code/Rust/toolbox/src/app.rs
rm /Users/lukewaehner/Code/Rust/toolbox/src/backups/main.bk.rs
rmdir /Users/lukewaehner/Code/Rust/toolbox/src/backups
```

Step 3 — Verify the backups directory is gone and cargo check passes:
`ls /Users/lukewaehner/Code/Rust/toolbox/src/backups/ 2>&1`

Step 4 — Run the full compile check:
`cargo check 2>&1 | grep "error\[" | head -20`

If cargo check fails after deletion, the most likely cause is a remaining reference to `crate::app::` or `mod app`. Run `grep -rn "mod app\|crate::app" src/` to identify and fix.
  </action>
  <verify>Run `cargo check 2>&1`. Confirm: (1) zero errors, (2) `ls src/app.rs 2>&1` says "No such file", (3) `ls src/backups/ 2>&1` says "No such file or directory", (4) `grep -rn "mod app\|crate::app" src/` returns zero matches.</verify>
  <done>src/app.rs deleted. src/backups/ directory removed. No mod app declaration in main.rs. No crate::app:: references anywhere. cargo check passes with zero errors. main.rs is now the sole entry point.</done>
</task>

</tasks>

<verification>
After both tasks, the following must all be true:
1. `cargo check` passes with zero errors
2. `ls src/app.rs` — No such file
3. `ls src/backups/` — No such file or directory
4. `grep -rn "mod app\|crate::app" src/` — zero matches
5. `grep -n "pub(crate) fn prepare_status_message" src/main.rs` — finds the declaration
6. `grep -rn "crate::prepare_status_message\|use crate::prepare_status_message" src/modules/` — finds the updated references

Full phase check — all five legacy files deleted:
7. `ls src/task_scheduler.rs src/network_tools.rs src/password_manager.rs src/system_utilities.rs src/app.rs` — all "No such file"
8. `grep -n "^mod task_scheduler\|^mod network_tools\|^mod password_manager\|^mod system_utilities\|^mod app" src/main.rs` — zero matches
</verification>

<success_criteria>
app.rs and backups/ deleted; mod app removed; all crate::app:: references replaced with crate:: path; cargo check clean. Phase 2 complete: single entry point (main.rs), all imports through src/modules/ controllers.
</success_criteria>

<output>
After completion, create `.planning/phases/02-architecture-consolidation/02-03-SUMMARY.md`
</output>
