---
phase: 02-architecture-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/task_scheduler/model/mod.rs
  - src/modules/task_scheduler/controller/mod.rs
  - src/main.rs
  - src/task_scheduler.rs
autonomous: true
requirements:
  - ARCH-04

must_haves:
  truths:
    - "src/task_scheduler.rs is deleted"
    - "All types used by main.rs and the task_scheduler controller are sourced from src/modules/task_scheduler/model/"
    - "cargo check passes with no new errors after the changes"
  artifacts:
    - path: "src/modules/task_scheduler/model/mod.rs"
      provides: "Canonical task_scheduler types (EmailConfig, SmsConfig, ReminderType, TaskPriority, TaskStatus, TaskScheduler, format_timestamp, run_scheduler_background_thread)"
      contains: "pub struct TaskScheduler"
    - path: "src/main.rs"
      provides: "Updated crate root with task_scheduler imports from modules path"
      contains: "use crate::modules::task_scheduler"
    - path: "src/modules/task_scheduler/controller/mod.rs"
      provides: "Updated controller with no legacy crate::task_scheduler:: references"
  key_links:
    - from: "src/main.rs"
      to: "src/modules/task_scheduler/model/mod.rs"
      via: "use crate::modules::task_scheduler::model"
      pattern: "use crate::modules::task_scheduler"
    - from: "src/modules/task_scheduler/controller/mod.rs"
      to: "src/modules/task_scheduler/model/mod.rs"
      via: "use crate::modules::task_scheduler::model"
      pattern: "crate::modules::task_scheduler"
---

<objective>
Fix the task_scheduler module so it is the sole canonical source for all task scheduler types, then delete the legacy src/task_scheduler.rs root file.

Purpose: task_scheduler.rs is the only SAFE-TO-NOT-DELETE file among the legacy roots because src/modules/task_scheduler/model/mod.rs already contains all the legacy content (copied verbatim). The blocker is a naming collision: mod.rs defines TaskPriority, TaskStatus, ReminderType, TaskScheduler, format_timestamp, and run_scheduler_background_thread inline AND re-exports identically-named items from task.rs and scheduler.rs — causing duplicate type definitions. This plan resolves that collision, rewires imports, and deletes the legacy file.

Output: src/task_scheduler.rs deleted, all task_scheduler types sourced exclusively from src/modules/task_scheduler/model/, cargo check passes.
</objective>

<execution_context>
@/Users/lukewaehner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lukewaehner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audit/AUDIT.md
@.planning/phases/01-audit/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve duplicate type definitions in src/modules/task_scheduler/model/mod.rs</name>
  <files>src/modules/task_scheduler/model/mod.rs</files>
  <action>
Read src/modules/task_scheduler/model/mod.rs in full. It currently has two problems:

PROBLEM A — Duplicate enum definitions: mod.rs defines TaskPriority, TaskStatus, and ReminderType inline (lines 23–46) AND re-exports them via `pub use task::{Task, TaskPriority, TaskStatus, ReminderType, Reminder};` (line 20). Remove the three inline enum definitions (TaskPriority, TaskStatus, ReminderType) from mod.rs — they are already defined in src/modules/task_scheduler/model/task.rs and re-exported by the `pub use task::` line.

PROBLEM B — Duplicate TaskScheduler: mod.rs defines its own `pub struct TaskScheduler` inline (with email_config and sms_config fields, full SMS/email methods, format_timestamp, run_scheduler_background_thread) AND re-exports `pub use scheduler::{TaskScheduler, SchedulerError, run_scheduler_background_thread};` (line 21). The inline TaskScheduler is the functionally complete version (has send_reminder_email, test_email_config, send_sms_reminder, create_smtp_transport, check_reminders, etc.). The scheduler.rs TaskScheduler is a clean rewrite but a stub (background thread is a placeholder only).

Resolution for PROBLEM B: Remove the re-export line `pub use scheduler::{TaskScheduler, SchedulerError, run_scheduler_background_thread};` from mod.rs. The inline TaskScheduler in mod.rs becomes the canonical one. Keep the `pub mod scheduler;` declaration (so the module compiles), but do NOT re-export from it. The scheduler.rs file remains on disk for future Phase 3+ work but is no longer the exported type.

After removing the duplicate enums and the scheduler re-export, mod.rs will have:
- `pub mod task;` and `pub mod scheduler;` declarations
- `pub use task::{Task, TaskPriority, TaskStatus, ReminderType, Reminder};` re-export
- EmailConfig, SmsConfig structs inline
- TaskScheduler struct inline (with all methods: send_reminder_email, test_email_config, send_sms_reminder, create_smtp_transport, check_reminders, mark_reminder_as_sent, new, add_task, update_task, delete_task, get_task, get_all_tasks, get_pending_tasks, add_reminder_to_task, set_email_config, set_sms_config, get_sms_gateway_email, save_email_config, load_email_config, save_sms_config, load_sms_config, load_tasks, save_tasks, send_desktop_notification)
- format_timestamp function inline
- run_scheduler_background_thread function inline
- All original use/import lines at the top of the file

Do NOT remove or change any of the function bodies — only remove the duplicate type definitions and the conflicting re-export line.
  </action>
  <verify>Run `cargo check 2>&1 | grep "error\[" | head -20` from the project root. The goal is zero "duplicate" or "redefined" errors related to TaskPriority, TaskStatus, ReminderType, or TaskScheduler. There may be other pre-existing warnings — those are acceptable.</verify>
  <done>cargo check reports zero errors about duplicate definitions in src/modules/task_scheduler/model/mod.rs. The types TaskPriority, TaskStatus, ReminderType, EmailConfig, SmsConfig, TaskScheduler, format_timestamp, and run_scheduler_background_thread are all accessible from the crate::modules::task_scheduler::model path.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire all crate::task_scheduler imports to crate::modules::task_scheduler::model</name>
  <files>src/main.rs, src/modules/task_scheduler/controller/mod.rs</files>
  <action>
Update all import sites that reference the legacy crate::task_scheduler path.

IN src/main.rs — make these changes:
1. Remove `mod task_scheduler;` (line 22) — this `mod` declaration is what pulls in src/task_scheduler.rs as a module. Removing it is what allows deletion of the file.
2. Replace `use crate::task_scheduler::{EmailConfig, ReminderType, TaskPriority, TaskScheduler, TaskStatus};` (line 25) with:
   `use crate::modules::task_scheduler::model::{EmailConfig, ReminderType, TaskPriority, TaskScheduler, TaskStatus};`

IN src/modules/task_scheduler/controller/mod.rs — make these changes:
1. Replace `use crate::task_scheduler::{EmailConfig, ReminderType, SmsConfig};` (line 3) with:
   `use crate::modules::task_scheduler::model::{EmailConfig, ReminderType, SmsConfig};`
2. Replace all `crate::task_scheduler::TaskPriority` references (lines 106, 116) with `crate::modules::task_scheduler::model::TaskPriority`
3. Replace `crate::task_scheduler::TaskStatus::Completed` (line 157) with `crate::modules::task_scheduler::model::TaskStatus::Completed`
4. Replace `crate::task_scheduler::EmailConfig {` (line 349) with `crate::modules::task_scheduler::model::EmailConfig {`
5. Replace all `crate::task_scheduler::ReminderType::` references (lines 415–419) with `crate::modules::task_scheduler::model::ReminderType::`

After both files are updated, verify no remaining `crate::task_scheduler` references exist:
`grep -rn "crate::task_scheduler" src/`

Expected result: zero matches.
  </action>
  <verify>Run `cargo check 2>&1 | grep "error\[" | head -20`. Also run `grep -rn "crate::task_scheduler" /Users/lukewaehner/Code/Rust/toolbox/src/` to confirm no legacy references remain.</verify>
  <done>cargo check shows no errors related to crate::task_scheduler. grep finds zero remaining crate::task_scheduler references in src/.</done>
</task>

<task type="auto">
  <name>Task 3: Delete src/task_scheduler.rs</name>
  <files>src/task_scheduler.rs</files>
  <action>
Delete the file src/task_scheduler.rs.

Command: `rm /Users/lukewaehner/Code/Rust/toolbox/src/task_scheduler.rs`

Then run cargo check to confirm the binary still compiles after the deletion.
  </action>
  <verify>Run `cargo check 2>&1 | grep "error\[" | head -20`. Run `ls /Users/lukewaehner/Code/Rust/toolbox/src/task_scheduler.rs 2>&1` to confirm the file is gone.</verify>
  <done>src/task_scheduler.rs does not exist. cargo check reports zero errors (warnings are acceptable — the pre-existing 22 warnings may remain or decrease).</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `cargo check` passes with zero errors
2. `ls src/task_scheduler.rs` returns "No such file"
3. `grep -rn "crate::task_scheduler" src/` returns zero matches
4. `grep -rn "use crate::modules::task_scheduler::model" src/main.rs src/modules/task_scheduler/controller/mod.rs` shows the updated imports
</verification>

<success_criteria>
src/task_scheduler.rs deleted; all imports of task_scheduler types route through crate::modules::task_scheduler::model; cargo check clean.
</success_criteria>

<output>
After completion, create `.planning/phases/02-architecture-consolidation/02-01-SUMMARY.md`
</output>
